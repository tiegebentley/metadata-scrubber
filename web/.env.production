# Inlined into the client bundle at `next build`. The browser uploads straight
# to the API so the Next.js proxy's 10 MB body cap and 30 s timeout never apply.
NEXT_PUBLIC_SCRUBMETA_API=http://100.79.168.65:8770
