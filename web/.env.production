# Inlined into the client bundle at `next build`. The browser uploads straight to
# the API (HTTPS via tailscale serve), so no proxy body cap or timeout applies.
NEXT_PUBLIC_SCRUBMETA_API=https://ubuntu-16gb-ash-1.tail9b86f6.ts.net:8443
